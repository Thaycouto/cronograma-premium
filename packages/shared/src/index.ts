export const NETLIFY_LANDING_URL = "https://couto-hair-program.netlify.app";
export const NETLIFY_WEBAPP_URL = "https://couto-hair-app.netlify.app";

export function normalizeEmail(email: string) {
  return email.trim().toLowerCase();
}
